{
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@scrap-it/backend",
  "outputDirectory": "apps/backend/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
